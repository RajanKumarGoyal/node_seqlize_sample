<section>
    <h2>Users List</h2>

    {{#if message}}
        <div class="alert {{messageClass}}" role="alert">
            {{message}}
        </div>
    {{/if}}

    <table class="table table-bordered" id="mytable">
        <thead>
            <tr>
                <th>Sr No.</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Email</th>
            </tr>
        </thead>
        <tbody>
            {{#each results}}
            <tr>
                <td>{{ id }}</td>
                <td>{{ firstName }}</td>
                <td>{{ lastName }}</td>
                <td>{{ email }}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>

</section>